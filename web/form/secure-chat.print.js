/* ══════════════════════════════════════════════════════════════════════
   PLACEHOLDER — this is not the real secure-chat.print.js.

   The real file defines `MedixlyPrint`, which builds the print/fax
   submission sheet. Nothing in `core`, `auth` or `demo` calls it, so an
   empty file is enough to keep the page from requesting one that isn't
   there. No print sheet can be generated until the real file lands.

   Per HANDOFF.md, the real one is pure black on white and deliberately
   ignores the design system — fax is 1-bit at 204x98 dpi.

   Replace this whole file. Do not merge into it.
   ═══════════════════════════════════════════════════════════════════ */
